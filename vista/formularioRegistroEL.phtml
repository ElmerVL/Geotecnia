<div id="admin_wrapper">
    <form action="" method="post" name="formEL">
        <div class="form-group">
            <label>Proyecto:</label>
            <select class="form-control" name="proyectoEL">
                <?php for ($i = 0; $i < sizeof($listaProyectoELSinCliente); $i = $i + 7) {?>
                    <option value="<?php echo $listaProyectoELSinCliente[$i]; ?>">
                        <?php echo $listaProyectoELSinCliente[$i + 2].' - '.$listaProyectoELSinCliente[$i + 1]; ?>
                    </option>
                <?php } ?>
            </select>
        </div>
        <p>
            <input type="hidden" name="grabarEL" value="si" />
            <input type="button" value="Aceptar" class="btn btn-default" onclick="document.formEL.submit();"/>
        </p>
    </form>
</div>